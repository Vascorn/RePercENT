wandb:
  project: "irfl-v2-3m"

M: 3  # number of modalities

# Use the same model configuration for each modality, except for the positional encoding parameters and input channels which are modality-specific
shared_encoder:
  # ---- gMLP Encoder ----
  type: "gMLP" 
  d_model: [768, 768, 512, 512, 512, 512] # This corresponds to the d_model dimension in the gMLP implementation => token embedding dimension
  d_ff: [352, 352, 352, 352, 352, 352] # This corresponds to the Feedforward hidden dimension in the gMLP implementation
  proj_h: [[768, 256], [768, 256], [512, 256], [512, 256], [512, 256], [512,256]] # Projection heads for the shared encoders to ensure output dimensions are the same across layers. Only relevant for gMLP case. In None, defaults to identity projections.
  seq_len: [49, 49, 77, 77, 77, 77] # This corresponds to the sequence length dimension in each gMLP block, and is modality-specific.
  num_layers: [2, 2, 2, 2, 2, 2] # number of gMLP layers per shared encoder


unique_encoder:
  # ---- gMLP Encoder ----
  type: "gMLP" 
  d_model: [768, 768, 512, 512, 512, 512]
  d_ff: [352, 352, 352, 352, 352, 352]
  proj_h: [[768, 256], [768, 256], [512, 256], [512, 256], [512, 256], [512,256]] # Projection heads for the unique encoders to ensure output dimensions are the same across layers. Only relevant for gMLP case. In None, defaults to identity projections.
  seq_len: [49, 49, 77, 77, 77, 77]
  num_layers: [2, 2, 2, 2, 2, 2] 


vmfkappa: 3000
add_shared: false
